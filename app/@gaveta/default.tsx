/**
 * Nada na "janela de gaveta" enquanto ninguém abriu um doce.
 *
 * Este arquivo é obrigatório: sem ele, o Next não sabe o que desenhar nessa
 * fatia do layout nas telas normais, e a navegação quebra ao atualizar a
 * página.
 */
export default function SemGaveta() {
  return null;
}
